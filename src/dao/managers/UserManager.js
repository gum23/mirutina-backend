
export default class UserManager{
    constructor(name, lastName, province, city, email, password){
        this.name = name,
        this.lastName = lastName,
        this.province = province,
        this.city = city,
        this.email = email,
        this.password = password
    }
}